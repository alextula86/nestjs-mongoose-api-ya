export type UserViewModel = {
  /**
   * id of existing user
   * login of existing user
   * email of existing user
   * createdAt of existing user
   */
  id: string;
  login: string;
  email: string;
  // passwordHash: string
  // passwordSalt: string
  createdAt: string;
};
